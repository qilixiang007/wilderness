global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'wilderness-backend'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # 同一 compose 网络内直连容器名,不依赖 host.docker.internal
      - targets: ['backend:8080']
